<?xml version="1.0" encoding="UTF-8"?>
<UserControl x:Class="SturmProjekt.Views.ProfileEditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"        
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             xmlns:custom="clr-namespace:SturmProjekt.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <Grid>
        <StackPanel>
            <Button Margin="0,10,0,10" Padding="3, 10, 30, 10" Command="{Binding NewProfileCommand}" Content="Neues Profil anlegen"></Button>
            <Button Margin="0,10,0,10" Padding="3, 10, 30, 10" Command="{Binding OpenProfileCommand}" Content="Profil bearbeiten"></Button>

            <TextBox Margin="0,10,0,10" Padding="3, 10, 30, 10" Name="ProfileName"  Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <TextBox Margin="0,10,0,10" Padding="3, 10, 30, 10" Name="PageCount"  Text="{Binding PageCount, UpdateSourceTrigger=PropertyChanged}"></TextBox>
        </StackPanel>
    </Grid>
</UserControl>